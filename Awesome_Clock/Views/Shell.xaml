<Window x:Class="Awesome_Clock.Views.Shell"        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Awesome_Clock.Views"
        mc:Ignorable="d"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"        
        Title="{Binding Title}" Height="450" Width="800" Background="#FF130101">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="35"></Setter>
            <Setter Property="Foreground" Value="White"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding Clock.CurrentTime.Hour, StringFormat={}{0:00}}"></TextBlock>
            <TextBlock Text=":"></TextBlock>
            <TextBlock Text="{Binding Clock.CurrentTime.Minute, StringFormat={}{0:00}}"></TextBlock>
            <TextBlock Text=":"></TextBlock>
            <TextBlock Text="{Binding Clock.CurrentTime.Second, StringFormat={}{0:00}}"></TextBlock>
        </StackPanel>
    </Grid>
</Window>
